<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns:context="http://www.springframework.org/schema/context"
  xmlns:mvc="http://www.springframework.org/schema/mvc"
  xsi:schemaLocation="
          http://www.springframework.org/schema/beans
          http://www.springframework.org/schema/beans/spring-beans.xsd
          http://www.springframework.org/schema/context
          http://www.springframework.org/schema/context/spring-context.xsd
          http://www.springframework.org/schema/mvc
          http://www.springframework.org/schema/mvc/spring-mvc.xsd">

  <!-- コンポーネントスキャンで Controller, Service, Repository を自動検出 -->
  <context:component-scan base-package="raisetech.StudentManagement"/>

  <!-- Spring MVC のアノテーション駆動を有効化 -->
  <mvc:annotation-driven/>

  <!-- StudentController を Bean として登録 -->
  <bean id="studentController" class="raisetech.StudentManagement.controller.StudentController">
    <constructor-arg ref="studentService"/>
  </bean>

  <!-- StudentService を Bean として登録（実装クラスがある想定） -->
  <bean id="studentService" class="raisetech.StudentManagement.service.StudentServiceImpl"/>

  <!-- 例外ハンドリングを SimpleMappingExceptionResolver で設定する例 -->
  <bean class="org.springframework.web.servlet.handler.SimpleMappingExceptionResolver">
    <property name="exceptionMappings">
      <props>
        <prop key="raisetech.StudentManagement.TestException">error/testError</prop>
        <prop key="java.io.IOException">error/ioError</prop>
      </props>
    </property>
    <property name="defaultErrorView" value="error/defaultError"/>
  </bean>

  <!-- ViewResolver（例: JSP 用） -->
  <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
    <property name="prefix" value="/WEB-INF/views/"/>
    <property name="suffix" value=".jsp"/>
  </bean>

</beans>
